#!/bin/bash



cd "$(dirname "$0")/.."
OUT="benchmarks/output/sumeuler_seq_10000.txt"
echo "SumEuler Sequential N=10000 - $(date)" > "$OUT"

for ((i=0 ; i < 10 ; i++)); do
    erl -noinput -eval "parSumEuler2:run_seq(10000), init:stop()." -pa benchmarks >> "$OUT" 2>&1
done
