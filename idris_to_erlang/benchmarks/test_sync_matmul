#!/bin/bash
# Quick sanity check for parMatMul_sync -- small input, single run, 2 workers.

cd "$(dirname "$0")/.."
erl -noinput -eval "parMatMul_sync:run(2, 50), init:stop()." -pa benchmarks -pa runtime -pa generated_sync
