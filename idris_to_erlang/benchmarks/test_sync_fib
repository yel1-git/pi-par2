#!/bin/bash
# Quick sanity check for fib_sync -- small input, single run, 2 workers.

cd "$(dirname "$0")/.."
erl -noinput -eval "fib_sync:runFibDC(2, 10, 20), init:stop()." -pa benchmarks -pa runtime -pa generated_sync
