#!/bin/bash



cd "$(dirname "$0")/.."
OUT="benchmarks/output/sumeuler_seq_20000.txt"
echo "SumEuler Sequential N=20000 - $(date)" > "$OUT"

for ((i=0 ; i < 10 ; i++)); do
    erl -noinput -eval "parSumEuler2:run_seq(20000), init:stop()." -pa benchmarks >> "$OUT" 2>&1
done
